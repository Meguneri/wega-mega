namespace Content.Shared._Wega.Duel;

/// <summary>
/// Маркер: анимацию RSI-спрайта этой сущности держать в ОДНОЙ фазе со всеми остальными такими же
/// сущностями. Нужен для арены — одинаковые анимированные киберпанк-стены должны мерцать в едином ритме
/// независимо от расстояния и от того, когда каждая вошла в поле зрения (обычно RSI-анимация стартует со
/// своего момента входа в PVS, поэтому стены мерцают вразнобой). Обрабатывается только на клиенте
/// (<c>SyncedSpriteAnimationSystem</c> в Content.Client), но компонент общий, чтобы его можно было указать
/// в прототипе. Собственных данных не несёт.
/// </summary>
[RegisterComponent]
public sealed partial class SyncedSpriteAnimationComponent : Component
{
}
