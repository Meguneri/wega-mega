# TODO — wega-mega

## Катана Рэнгоку (ArenaRengokuKatana)

- [ ] **Нарисовать кастомные кадры эффектов.** Эффекты доработаны компоновкой
  ванильных слоёв (`Effects/explosion.rsi` + `Effects/fire.rsi` + искры
  `Effects/sparks.rsi`) и кодом: Первая форма рисует веер пламени перед
  носителем, Девятая — огненный след вдоль рывка и кольцо взрыва (см.
  `Resources/Prototypes/_Wega/Entities/Effects/rengoku_katana.yml` и
  `RengokuKatanaSystem`). Выглядит уже прилично, но для идеала всё ещё хочется
  собственные нарисованные кадры под огненные приёмы, а не свод ванильных.
- [ ] **Переделать звук удара.** Текущий `Audio/_Wega/Weapons/rengoku_slash.ogg`
  — авто-свод `bladeslice` + `fire` (см. `Tools/gen_rengoku_slash_sound.sh`).
  Хочется нормальный звук пореза+огня, а не миксованный на коленке.

## Арена / Рейд-режим — точка продолжения (2026-07-12)

Сделано:
- [x] Интеграционные тесты hideout (`RaidControllerTest`) — 8/8 проходят.
- [x] `RaidStashSystem` — телепорт на базу по `PlayerAttachedEvent`, `runMapInit: true`,
      кэш координат спавна, очистка при дисконнекте/рестарте.
- [x] H.E.C.K. шлем и костюм — отдельные листинги в Full Arsenal, нет совместного спавна.
- [x] Большой модуль хранения убран из пула синдикатских бандлов.
- [x] Пагинация медиа-плеера по 10 треков.
- [x] Миньон затаённой обиды — `FlyingMobLayer`, пустой `mask`.
- [x] Шторм Arena31 — `SharedPvsOverrideSystem` для глобального оверрайда.

С чего продолжить завтра:
- [ ] **Живой прогон рейда.** Проверить коннект → база → вход → лут/скавы → экстракт → база.
      Использовать `RaidControllerTest` для быстрого старта или сделать свою карту.
- [ ] **H.E.C.K. шлем** — если всё ещё спавнится поверх ящика / не удаляется после дуэли,
      смотреть рантайм-логи спавна `StoreSystem`, не прототипы.
- [ ] **Шторм Arena31** — перепроверить визуальную зону вживую; возможно, нужен дополнительный
      PVS override на сущность оверлея.
- [ ] **Дисконнект во время рейда** — проверить, что рейдер возвращается на хаб/базу без лута.
- [ ] **Полная сборка + YAMLLinter + все интеграционные тесты** перед коммитом.

Подробности и ссылки на файлы — в `dev/RAID_MODE.md` (раздел «Наработки от 2026-07-12»).
